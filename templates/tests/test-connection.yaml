apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "vault-kms-plugin-deploy.fullname" . }}-test-connection"
  labels:
{{ include "vault-kms-plugin-deploy.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "vault-kms-plugin-deploy.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
